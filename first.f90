program first

    ! implicit none mean deacleare all variables i think
    implicit none

    real :: a,b, result

    a = 12
    b = 15

    result = a + b
    print*, 'total sum is ', result
    print*, "fortran seems good"

end program first